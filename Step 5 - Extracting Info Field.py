info_field = []
    numbers = [0,1]
    for i in range(len(data)):
        info_field.append(data[i]['INFO'])
    return info_field


"""

a function that extracts the info field from the data dictionary that was 
    created in the previous part. The function should return all the info field dictionaries as list.
    
"""
